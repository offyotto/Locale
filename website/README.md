# Locale Website

Static marketing site for Locale.

## Preview

Open `index.html` directly in a browser, or serve it locally:

```bash
python3 -m http.server 4173 --directory website
```

Then open:

```text
http://localhost:4173
```

## Deploy

The repository includes a GitHub Pages workflow that publishes the `website/`
directory on pushes to `main`.

